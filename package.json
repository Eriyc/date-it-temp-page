{
  "private": true,
  "scripts": {
    "watch": "tailwindcss -i ./styles.css -o ./output.css --watch",
    "build": "mkdir build && cp output.css build/output.css && cp index.html build/index.html"
  },
  "dependencies": {
    "@vercel/edge": "^0.1.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  }
}
